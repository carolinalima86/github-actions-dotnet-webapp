name: Teste simples - Atividade Modulo 4

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 * * * *'  # executa a cada hora

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Mensagem de sucesso
        run: echo "Workflow executado com sucesso! 🎉"
